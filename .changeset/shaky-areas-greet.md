---
---

Add ESLint configuration with ANTFU config as shared `@haven/eslint-config` package. This establishes linting and formatting standards for the monorepo (chore/tooling change, no version bump needed).
